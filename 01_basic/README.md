npm create vite@latest

cd that
in github create a repo

local git init
git commit -m "initial commit"
git remote add origin https://github repo


git remote set-url origin https://github.com
 to update the orgin 

git branch -M main // rename
git push -u origin main // -u is set upstream

Babel is a JavaScript compiler (or “transpiler”).